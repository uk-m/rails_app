<% provide(:title, '新規登録') %>
<div class="row justify-content-center">
  <div class="col-md-6 text-center">
    <h1 class="logo">MonoLog</h1>
    <h2>みんなのおすすめ商品をチェックしよう</h2>
    <%= form_with scope: resource, as: resource_name, url: registration_path(resource_name), local: true do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>
      <div class="mb-3">
        <%= f.email_field :email, autofocus: true, placeholder: "メールアドレス", class: "form-control" %>
      </div>
      <div class="mb-3">
        <%= f.text_field :name, autofocus: true, placeholder: "ユーザーネーム", class: "form-control" %>
      </div>
      <div class="mb-3">
        <%= f.text_area :self_introduction, autofocus: true, placeholder: "自己紹介文", class: "form-control" %>
      </div>
      <div class="mb-3">
        <%= f.password_field :password, autocomplete: "off", placeholder: "パスワード", class: "form-control" %>
      </div>
      <div class="mb-3">
        <%= f.password_field :password_confirmation, autocomplete: "off", placeholder: "パスワードの確認", class: "form-control" %>
      </div>
      <div class="mb-3">
        <%= f.submit "登録する", class: "btn btn-primary w-100" %>
      </div>
    <% end %>
    <p class="mb-3">
      アカウントをお持ちですか？
      <%= link_to "ログインする", new_user_session_path %>
    </p>
    <div>
      <%= link_to user_google_oauth2_omniauth_authorize_path, method: :post do %>
        <i class="fab fa-google"></i> googleアカウントでログイン
      <% end %>
    </div>
  </div>
</div>