<% provide(:title, 'パスワード再送') %>
<div class="row justify-content-center">
  <div class="col-md-6 text-center">
    <h1 class="logo">MonoLog</h1>
    <h2 class="mb-3">パスワードをお忘れですか?</h2>
    <%= form_with scope: resource, as: resource_name, url: password_path(resource_name), local: true do |f| %>
      <%= devise_error_messages! %>
      <div class="mb-3">
        <%= f.email_field :email, autofocus: true, placeholder: "メールアドレス", class: "form-control" %>
      </div>
      <div class="mb-3">
        <%= f.submit "パスワードを再送する", class: "btn btn-outline-primary" %>
      </div>
    <% end %>
    <p class="mb-3">
      アカウントをお持ちでないですか？
      <%= link_to "登録する", new_user_registration_path %>
    </p>
    <p class="mb-3">
      アカウントをお持ちですか？
      <%= link_to "ログインする", new_user_session_path %>
    </p>
  </div>
</div>

