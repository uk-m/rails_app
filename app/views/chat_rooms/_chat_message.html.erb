<div>
  <% if chat_message.user.id != current_user.id %>
    <div class="border border-secondary justify-content-start">
      <%= chat_message.content %>
    </div>
  <% else %>
    <div class="border border-secondary justify-content-end">
      <%= chat_message.content %>
    </div>
  <% end %>
</div>