<div class="row justify-content-center">
  <div class="col-md-6 text-center">
    <h2>投稿画面</h2>
    <%= form_with model: @post, class: "upload-images p-0 border-0" do |f| %>
      <div class="mb-3">
        <%= f.fields_for :photos do |i| %>
          <%= i.file_field :image %>
        <% end %>
      </div>
      <div class="form-group">
        <div class="field">
          <%= f.text_area :content, class: "form-control", placeholder: "説明を記載" %>
        </div>
      </div>
      <div class="form-group">
        <h3 class="m-3">カテゴリー</h3>
        <%= f.collection_select :tag_id, Tag.all, :id, :name, prompt: "選択してください" %>
      </div>
      <%= f.submit "投稿する", class: "btn btn-primary m-3" %>
    <% end %>
  </div>
</div>
<%
=begin %>
        <%= f.collection_check_boxes(:tag_ids, Tag.all, :id, :name) do |tag| %>
          <div class="form-check">
            <%= tag.label class: 'form-check-label form-check-inline' do %>
              <%= tag.check_box class: 'form-check-input' %>
              <%= tag.text %>
            <% end %>
          </div>
        <% end %>
<%
=end %>